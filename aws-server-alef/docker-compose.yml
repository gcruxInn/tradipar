version: '3.8'

services:
  api-precos:
    build: .
    container_name: api-precos-sankhya
    restart: unless-stopped
    ports:
      - "3005:3000"
    env_file:
      - .env
    logging:
      driver: "json-file"
      options:
        max-size: "10m"
        max-file: "3"
